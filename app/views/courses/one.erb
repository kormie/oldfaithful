<% all_courses = Array.new %>
<% for department in @departments %>
  <% department.courses.each do |course, | %>
    <% all_courses << course.department.name + " " + course.number.to_s %>
  <% end -%>
<% end -%>
<%= all_courses %>